import { StyleSheet } from "react-native";
import { colors, radius, spacing } from "../../../styles.theme/theme";

export const Styles = StyleSheet.create({
  productItemCard: {
    borderWidth: 1,
    borderColor: colors.border,
    padding: spacing.md,
    borderRadius: radius.md,
  },
});
